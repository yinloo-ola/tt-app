package user

templ UserModal(action string, userForm templ.Component) {
	<div class="-translate-y-50% relative top-50% w-75% rounded-lg bg-amber-1 p-4">
		<h3>
			if action == "new" {
				Add New User
			} else if action =="update" {
				Update
			}
		</h3>
		@userForm
	</div>
}
